# Terminal Launcher

Personal Android home screen launcher with terminal UI.

## Commands
- `run <app>` - launch or install app
- `delete <app>` - remove app from launcher
- `list` - show all installed apps
- `open launcher.setting` - settings
- `clear` - clear terminal
- `help` - show commands

## Build
APK is built automatically via GitHub Actions on every push.
